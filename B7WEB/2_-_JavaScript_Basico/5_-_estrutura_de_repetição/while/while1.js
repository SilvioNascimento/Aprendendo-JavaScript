let num = 1;

while(num <= 10) {
    console.log(`O número da vez é ${num}`)
    num++;  // É recomendado utilizar essa incremento da variável 'num' para evitar que
            // o while possa repetir o comando infinitamente
}